apiVersion: v1
kind: ConfigMap
metadata:
  name: snapshot-cleaner-config
  namespace: petrobras-infra-tools
data:
  SNAPSHOT_RETENTION_HOURS: "30" 
  EXCLUDED_VM_TAGS: "backup=permanente"
  DRY_RUN: "false"
  NAMESPACE: ""  # Deixe vazio para todos os namespaces